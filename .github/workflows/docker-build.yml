name: Build and Push Docker Images

on:
  workflow_call:
    inputs:
      version:
        description: 'Release version tag (e.g. v0.6.0)'
        required: true
        type: string
      also_latest:
        description: 'Also tag as latest (default: true)'
        required: false
        type: boolean
        default: true
  workflow_dispatch:

jobs:
  # Unified image - works on all GPUs with tested CUDA architectures
  # Uses pre-built engines from a2go-engines (built by engines-build.yml)
  # Builds per-architecture images (amd64 + arm64) then creates a multi-arch manifest.
  build-unified:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: blacksmith-4vcpu-ubuntu-2404
          - arch: arm64
            runner: blacksmith-4vcpu-ubuntu-2404-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - name: Set environment variables
        run: |
          echo "DOCKERHUB_REPO=${{ vars.DOCKERHUB_REPO || secrets.DOCKERHUB_USERNAME }}" >> $GITHUB_ENV

          # Determine Docker tag:
          #   workflow_call with version input → version (+ latest if also_latest)
          #   workflow_dispatch → latest
          if [[ -n "${{ inputs.version }}" ]]; then
            echo "VERSION=${{ inputs.version }}" >> $GITHUB_ENV
            echo "ALSO_LATEST=${{ inputs.also_latest }}" >> $GITHUB_ENV
          else
            echo "VERSION=latest" >> $GITHUB_ENV
            echo "ALSO_LATEST=false" >> $GITHUB_ENV
          fi

          echo "PUSH_IMAGES=true" >> $GITHUB_ENV

      - name: Login to Docker Hub
        if: env.PUSH_IMAGES == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: useblacksmith/build-push-action@v2
        with:
          context: .
          file: Dockerfile.unified
          push: ${{ env.PUSH_IMAGES == 'true' }}
          build-args: |
            ENGINES_IMAGE=${{ env.DOCKERHUB_REPO }}/a2go-engines:latest-${{ matrix.arch }}
          tags: |
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }}-${{ matrix.arch }}
            ${{ env.ALSO_LATEST == 'true' && format('{0}/a2go:latest-{1}', env.DOCKERHUB_REPO, matrix.arch) || '' }}
          platforms: linux/${{ matrix.arch }}

  create-manifest:
    needs: build-unified
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - name: Set environment variables
        run: |
          echo "DOCKERHUB_REPO=${{ vars.DOCKERHUB_REPO || secrets.DOCKERHUB_USERNAME }}" >> $GITHUB_ENV

          # Determine Docker tag (must match build job logic)
          if [[ -n "${{ inputs.version }}" ]]; then
            echo "VERSION=${{ inputs.version }}" >> $GITHUB_ENV
            echo "ALSO_LATEST=${{ inputs.also_latest }}" >> $GITHUB_ENV
          else
            echo "VERSION=latest" >> $GITHUB_ENV
            echo "ALSO_LATEST=false" >> $GITHUB_ENV
          fi

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push multi-arch manifest
        run: |
          docker buildx imagetools create -t \
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }} \
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }}-amd64 \
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }}-arm64

      - name: Also tag as latest
        if: env.ALSO_LATEST == 'true'
        run: |
          docker buildx imagetools create -t \
            ${{ env.DOCKERHUB_REPO }}/a2go:latest \
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }}-amd64 \
            ${{ env.DOCKERHUB_REPO }}/a2go:${{ env.VERSION }}-arm64
