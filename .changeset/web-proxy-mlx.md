---
"a2go": minor
---

Add unified web proxy to MLX backend, matching Docker's single-API-on-port-8080 architecture. Strip `:quant` suffix from model references for MLX. Rename `openclaw-web-proxy` to `web-proxy`. Remove legacy per-model `models/` directory.
